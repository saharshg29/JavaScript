var findTheWinner = function (n, k) {
  let list = [];
  for (let i = 1; i <= n; i++) {
    list.push(i);
  }
  while (list.length !== 1) {
    
  }
};

let n = 6,
  k = 5;

console.log(findTheWinner(n, k));
